# macOS
.DS_Store
__MACOSX/

# IDE / editor
.vscode/
.idea/
*.swp
*~

# Sentry secrets — never commit
sentry-secrets.md
.env
.env.local

# Build outputs (Surveillance has no build pipeline — manual ZXP packaging)
*.zxp

# Logs
*.log
logs/

# ZXPSignTool credentials (if any) — keep the tool but exclude any .p12 etc
*.p12
*.pfx
ZXPSignTool/*.p12
ZXPSignTool/*.pfx
ZXPSignTool/cert/
